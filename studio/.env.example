# Sanity CLI reads SANITY_STUDIO_-prefixed vars for the Studio app itself.
# Not secret — same project id/dataset used by the web app's NEXT_PUBLIC_ vars.
SANITY_STUDIO_PROJECT_ID=
SANITY_STUDIO_DATASET=
